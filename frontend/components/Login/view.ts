import { View } from '../view'
import './style.scss'
import { template } from './template'

export default class LoginView extends View {
  constructor() {
    super(template)
  }
  mount(): void {}
}
